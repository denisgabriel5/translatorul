# Copy to .env and fill in. .env is gitignored -- never commit real secrets.

# Optional Hugging Face token (read-only is enough). Enables higher rate
# limits / faster model downloads and silences the "unauthenticated requests
# to the HF Hub" warning. Get one at https://huggingface.co/settings/tokens
HF_TOKEN=
